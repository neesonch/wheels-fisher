import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  root: {
    padding: "5px",
    margin: "3px"
  },
  title: {
    textAlign: 'center'
  },
  value: {
    textAlign: 'center'
  }
});